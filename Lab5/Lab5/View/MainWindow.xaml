<Window x:Class="Lab5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:charting="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit"
        Title="MainWindow" Width="1000" Height="600"
        DataContext="{Binding Main, Source={StaticResource Locator}}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!--Charts-->
        <charting:Chart Name="Charts" Grid.Column="0" Margin="3">
            <charting:LineSeries 
                IndependentValuePath="X"
                DependentValuePath="Y"
                ItemsSource="{Binding ChartData}"/>
            <charting:Chart.Axes>
                <charting:LinearAxis
                    Orientation="X"
                    Title="X"
                    ShowGridLines="True"/>
                <charting:LinearAxis 
                    Orientation="Y"
                    Title="Y"
                    ShowGridLines="True"/>
            </charting:Chart.Axes>
        </charting:Chart>
        
        <!--Results-->
        <StackPanel Grid.Column="1">
            
            <TextBlock Text="Полученное значение интеграла:" 
                       Margin="10, 10, 10, 5"/>
            <TextBlock Text="{Binding gettedValue}"
                       Margin="20,0,0,5"/>
            
            <TextBlock Text="Истинное значение интеграла:" 
                       Margin="10, 10, 10, 5"/>
            <TextBlock Text="{Binding realValue}"
                       Margin="20,0,0,5"/>
            
        </StackPanel>
    </Grid>
</Window>
